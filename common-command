  docker compose up --build
  docker ps
   ip a
   ls ~/.ssh
   ssh -T git@github.com
   git init
   ls
   ls -l
   ls -la
   git add .
   git status
   git status oneline
   git add -A
   git status
   git commit -m "Initial commit with docker and html files"
   git status
   git remote add origin git@github.com:kapildhoundiyal7889-prog/docker-compose-.git
   git branch -M main
   git push -u origin main
   history
